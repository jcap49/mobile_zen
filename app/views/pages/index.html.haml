.section
  #login_container
    -unless user_signed_in?
      %button.tiny.secondary#login
        =link_to 'Login', new_user_session_path
    -if user_signed_in?
      %button.tiny.secondary#logout
        =link_to 'Logout', destroy_user_session_path, :method => "delete"
  .landing_header
    %h1.subheader#landing_title
      Welcome to Bonsai. 
    %h3.subheader
      Your daily cause for pause.
    %p#slug_description
      We deliver a daily SMS with a question or comment intended to give you reason to stop and reflect for a moment or three.
  #landing_image
    =image_tag 'bonsai1.jpg'
  -unless user_signed_in?
    #info_prompt
      %p 
        There's just a little bit of info needed first... 
  
  -if user_signed_in? && current_user.max_text_message_limit?(current_user.id)
    #text_limit
      %p#text_limit_message
        Unfortunately there's a one text message limit for user accounts at the moment. Feel free to reply with 'DELETE' to delete your current message - you can then create a new message of your choosing.
  -else  
    #landing_form
      =render 'text_messages/form', locale: {text_message: TextMessage.new}
  
    
    
  
